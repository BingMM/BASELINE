"""
Future home for array-first smoothing kernels used by `baseline_v2`.

The initial scaffold keeps smoothing delegated to the current reference
implementation. This module exists so new smoothing code lands in a stable
location as the compatibility layer is replaced.
"""

